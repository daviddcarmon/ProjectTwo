
<header class="masthead" id="duelApp">
    <div class="container d-flex h-100 align-items-center">
        <div class="mx-auto text-center">
            <h1 class="mx-auto my-0">Prepare to Duel</h1>
                        <section class="signup-section" id="signup">
                            <nav class="navbar navbar-light bg-light">
  <form class="form-inline">
    <a class="btn btn-primary" href="/" id="logoutBtn" >Main button</button>>
  </form>
</nav>
            
            <!-- DROPDOWN BUTTON -->
            <form action="">
                <select class="charList" name="test" id="">
                    <option value="" selected>Select</option>
                </select>
            </form>
            <div class="ranBtn">
            </div>
            <div class="winner"></div>
            <div class="userChar">
                <image class="userImage" src="{{character.image}}">


                    <div class="userName">
                        {{character.name}}
                    </div>
                    <div class="userHealth">
                        Health: {{character.health}}
                    </div>
                    <div class="userAttack">
                        Attack: {{character.attack}}
                    </div>
            </div>
            <div class="randChar"></div>
            <div class="play"></div>
            </section>
                        

            
            </header>
              <footer class="footer bg-black small text-center text-white-50">
      <div class="container">Gabriella Trujillo-Robnett | Brittanie Barrett | David Cameron | Aaron Howard</div>
    </footer>
            
